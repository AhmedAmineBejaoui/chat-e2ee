// Temporary placeholder component removed from routing.
// Kept as an empty component in case of future reuse.

import React from "react";

const LandingHero: React.FC = () => null;

export default LandingHero;
